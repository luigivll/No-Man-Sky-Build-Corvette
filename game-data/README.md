# Extracted game data

Drop `.EXML` files here (module definitions, and anything else useful) and they
can be imported into `data/parts.json`. See `docs/game-data-import.md`.

Small sets are easier to hand over as a chat attachment; use this folder when you
want to keep them, and push them on a branch.
